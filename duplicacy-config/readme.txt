add duplicacy repositories in this directory

init repositories with this cmd

/usr/local/bin/duplicacy init -e -pref-dir /root/.duplicacy-config/[dir being backed up] -storage-name B2-home-backup -repository [path to dir being backed ip] home-linux-[dir being backed up] b2://home-backup-duplicacy
set password on commandline and add keys section to prefs file when complete. add a comma after last value before keys section starts
